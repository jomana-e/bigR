% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/big_summarize.R
\name{big_summarize}
\alias{big_summarize}
\title{Summarize a large dataset efficiently}
\usage{
big_summarize(data, ...)
}
\arguments{
\item{data}{A dataframe or DuckDB connection}

\item{...}{Summarization expressions}
}
\value{
A summarized dataframe
}
\description{
Summarize a large dataset efficiently
}
\examples{
\dontrun{
# Basic summary statistics
summary <- big_summarize(data,
  mean_val = mean(value),
  sd_val = sd(value),
  count = n()
)

# Group-wise summary with big_group_by
summary <- data \%>\%
  big_group_by(category) \%>\%
  big_summarize(
    avg = mean(value),
    total = sum(value),
    count = n()
  )

# Using with DuckDB connection
con <- DBI::dbConnect(duckdb::duckdb())
summary <- big_summarize(con,
  "AVG(value) as mean_val",
  "COUNT(*) as count"
)
}
}
