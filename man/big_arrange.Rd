% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/big_arrange.R
\name{big_arrange}
\alias{big_arrange}
\title{Sort a large dataset efficiently}
\usage{
big_arrange(data, ...)
}
\arguments{
\item{data}{A dataframe or DuckDB connection}

\item{...}{Columns to sort by}
}
\value{
A sorted dataframe
}
\description{
Sort a large dataset efficiently
}
\examples{
\dontrun{
# Basic sorting
result <- big_arrange(data, value)

# Sort in descending order
result <- big_arrange(data, desc(value))

# Multiple sort columns
result <- big_arrange(data,
  category,
  desc(value),
  date
)

# Combining with other bigR functions
result <- data \%>\%
  big_filter(value > 0) \%>\%
  big_arrange(desc(value)) \%>\%
  big_select(date, value, category)

# Using with DuckDB connection
con <- DBI::dbConnect(duckdb::duckdb())
result <- big_arrange(con, "category ASC", "value DESC")
}
}
