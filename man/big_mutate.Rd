% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/big_mutate.R
\name{big_mutate}
\alias{big_mutate}
\title{Mutate columns in a large dataset efficiently}
\usage{
big_mutate(data, ...)
}
\arguments{
\item{data}{A dataframe or a database connection}

\item{...}{Mutate expressions}
}
\value{
The mutated dataset
}
\description{
Mutate columns in a large dataset efficiently
}
\examples{
\dontrun{
# Basic mutation
result <- big_mutate(data, new_col = col1 * 2)

# Multiple mutations
result <- big_mutate(data,
  new_col1 = col1 * 2,
  new_col2 = col2 + 10,
  ratio = col1 / col2
)

# Using with DuckDB connection
con <- DBI::dbConnect(duckdb::duckdb())
result <- big_mutate(con, transformed = log(value))
}
}
